# Nexus Registration Guide for {PROJECT_NAME}

> Generated by meta-metacognition factory. This guide helps register the project with the nexus runtime gateway.

## Prerequisites

The following files must exist for nexus to discover this project:

| File | Purpose | Status |
|------|---------|--------|
| `essence.md` | Project identity — what makes this valuable | Required |
| `.brain/config.yaml` | Project metadata (name, version, orchestrator) | Required |
| `.github/` | GitHub agents directory | Required |
| `.github/agents/{ORCHESTRATOR_NAME}.agent.md` | Orchestrator instructions | Required |
| `.brain/lessons.md` | Accumulated operational knowledge | Recommended |
| `.brain/status.md` | Session re-entrancy state | Recommended |
| `.brain/principles.md` | Engineering/domain principles | Recommended |
| `skills/` | Quality protocols | Optional (v3+) |

## Registration

Add this project to `nexus.config.yaml` under `wrappedProjects.projects`:

```yaml
- name: "{PROJECT_SLUG}"
  path: "{PROJECT_PATH}"
  protocolEnforcement:
    enabled: true
```

After v1 (shared preamble), the per-project preamble is composed dynamically from the project's content files. No copy-paste needed.

## Verification

1. Start nexus: `npm start` (from nexus directory)
2. Verify project loads: `nexus health` should list {PROJECT_NAME}
3. Start a session: `nexus chat --project {PROJECT_SLUG}`
4. Verify essence injection: the agent should demonstrate awareness of the project's identity
