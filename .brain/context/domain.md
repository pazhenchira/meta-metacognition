meta-metacognition is a meta-orchestrator ENGINE that builds applications via a 12-phase pipeline. The engine logic lives in `.brain/playbooks/build-from-intent.md`. The project is now managed using MetaAgent conventions.
